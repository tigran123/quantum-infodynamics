# Base runtime dependencies. Locked with:
#   uv pip compile requirements.in -o requirements.txt
fastapi
uvicorn[standard]
pydantic
numpy
scipy
sympy
pyfftw
# mp4 export: renders the video frames (Agg) off the already-computed
# history; the encoding itself is piped to the system ffmpeg
matplotlib
